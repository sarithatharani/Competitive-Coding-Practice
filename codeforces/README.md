# Codeforces

Solutions live here, one folder per problem:

```
codeforces/
└── <problem-number>-<problem-name>/
    ├── <ProblemName>.<ext>   # code
    └── README.md              # explanation + intuition
```
